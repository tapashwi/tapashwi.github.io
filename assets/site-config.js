// Site-wide settings. adsenseClient stays empty until this site runs on a domain you own
// (e.g. tools.tapaswibaskota.com.np via a CNAME file) and AdSense approves it; github.io
// subdomains cannot be approved. Set it to "ca-pub-..." and fill slot IDs to turn ads on.
window.CE_CONFIG = {
  mainSite: 'https://explainer.tapaswibaskota.com.np',
  adsenseClient: '',
  adSlots: { 'tool-mid': '', 'hub-mid': '' },
};
